# Markdownlint configuration for firedantic-extras
# See https://github.com/DavidAnson/markdownlint/blob/main/doc/Rules.md

# Disable line length rule - documentation needs longer lines for code examples
MD013: false

# Allow duplicate headings with different content
MD024:
  siblings_only: true

# Allow inline HTML (useful for alerts, tables, etc.)
MD033: false

# Allow raw URLs (useful in documentation)
MD034: false
